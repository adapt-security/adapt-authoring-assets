# adapt-authoring-assets

Manages assets (images, video, audio and other media) used in Adapt courses, and exposes them via a REST API.

Extends `AbstractApiModule` from [adapt-authoring-core](../adapt-authoring-core); asset binaries are handled separately from their metadata documents.

## Documentation

- [Assets](docs/assets.md)
